# Publish domma-celebrate to npm via trusted publishing (OIDC).
#
# No tokens, no one-time codes: GitHub mints a short-lived OIDC token for this
# exact workflow, and npm trusts it because the package is configured with a
# Trusted Publisher pointing back here. That replaces the granular-token path,
# which npm has withdrawn for direct publishing, and it is the route this
# repository uses - publishing by hand needs an interactive login and would
# hit EOTP on any account with 2FA on writes.
#
# Mirrors domma/.github/workflows/publish.yml,
# domma-reactive/.github/workflows/publish.yml and
# domma-cms/.github/workflows/publish.yml; keep the four in step.
#
# The first publish is done. 1.0.0 went out by hand on 2026-09-22, because a
# Trusted Publisher cannot be set on a package that does not exist yet and so
# the very first release can never be the OIDC one. The Trusted Publisher was
# configured straight after, which makes 1.0.1 onward an ordinary tag-and-go:
# push a v* tag and this workflow does the rest. Nothing to set up per release.
#
# Worth knowing if a run ever fails with ENEEDAUTH: that is the runner having
# no credentials of its own, not your local `npm whoami`. A local login is
# invisible here. It means the Trusted Publisher is missing or does not match
# the five values below exactly.
#
# npm side, configured 2026-09-22: npmjs.com -> domma-celebrate -> Settings
#   -> Trusted Publisher
#   Provider: GitHub Actions
#   Organization or user: pinpointzero73
#   Repository:           domma-celebrate
#   Workflow filename:    publish.yml
#   Environment:          (leave empty)
#
# NPM_TOKEN is consequently unset, and should stay that way: the Publish step
# below prefers a token when one exists, so adding one silently takes the
# OIDC path out of service.
name: Publish to npm

on:
  push:
    tags:
      - 'v*'
  # Lets an already-pushed tag be published without re-tagging.
  workflow_dispatch:
    inputs:
      ref:
        description: 'Tag or ref to publish (e.g. v1.0.0)'
        required: true

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write          # required for OIDC; without it npm sees no identity
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.inputs.ref || github.ref }}

      # Deliberately NO registry-url: setup-node would then write an .npmrc
      # containing `//registry.npmjs.org/:_authToken=${NODE_AUTH_TOKEN}`, and
      # with no NODE_AUTH_TOKEN in the environment that resolves to an empty
      # credential. npm sees a token-shaped auth config, takes the token path
      # instead of OIDC, and the registry rejects it as 404 on PUT. The default
      # registry is registry.npmjs.org anyway.
      - uses: actions/setup-node@v7
        with:
          node-version: '24'

      # Trusted publishing needs npm >= 11.5.1. setup-node ships whatever npm
      # came with the Node release, which is older - so upgrade explicitly
      # rather than relying on the runner image.
      - name: Use an npm that speaks OIDC
        run: npm install -g npm@latest

      # If either of these is wrong, npm silently falls back to token auth and
      # the failure looks like a permissions problem rather than a config one.
      - name: Check the OIDC prerequisites
        run: |
          echo "npm: $(npm --version)  (needs >= 11.5.1 for OIDC)"
          if [ -n "$ACTIONS_ID_TOKEN_REQUEST_URL" ]; then
            echo "OIDC token endpoint: present"
          else
            echo "::error::No OIDC endpoint - 'id-token: write' permission is missing"
            exit 1
          fi
          echo "--- effective npm auth config ---"
          npm config list | grep -iE 'registry|auth' || echo "(none - correct for OIDC)"

      - name: Install dependencies
        run: npm ci

      # The tag and package.json must agree, or a mistyped tag silently
      # publishes the wrong version.
      - name: Check the tag matches package.json
        run: |
          REF="${{ github.event.inputs.ref || github.ref_name }}"
          PKG="v$(node -p "require('./package.json').version")"
          echo "ref=$REF package.json=$PKG"
          if [ "$REF" != "$PKG" ]; then
            echo "::error::Tag $REF does not match package.json version $PKG"
            exit 1
          fi

      # `files` ships dist/, which is gitignored and so absent from a fresh
      # checkout. prepublishOnly would build it, but building explicitly means
      # a build failure is reported as one rather than as a publish failure.
      - name: Build the bundles the package ships
        run: npm run build

      # verify-dist checks the version stamp and that the themes are split into
      # chunks in the ESM build and inlined in the UMD one. Shipping the ESM
      # entry without its chunks would 404 every celebration on the day it
      # matters, which is exactly the failure a smoke test cannot see.
      - name: Check the bundles are actually there
        run: |
          for f in dist/domma-celebrate.esm.js dist/domma-celebrate.cjs dist/domma-celebrate.min.js; do
            test -s "$f" || { echo "::error::$f missing or empty - refusing to publish"; exit 1; }
          done
          test -d dist/chunks || { echo "::error::dist/chunks missing - the ESM build would 404 in season"; exit 1; }
          node scripts/verify-dist.mjs
          echo "all three bundles present, chunks intact, and verified"

      # After the build on purpose, to match the other three repositories: a
      # spec that loads the built bundle fails for want of a build rather than
      # for anything wrong with the code.
      - name: Run tests
        run: npm run test:run

      # Two routes to the registry, because one setting on npmjs.com is not
      # always reachable: OIDC needs a Trusted Publisher on the package, and an
      # automation token needs only the tokens page. Whichever exists is used.
      #
      # The token is written to .npmrc at publish time rather than through
      # setup-node's registry-url, because that would write the same file at
      # SETUP time - and an empty NODE_AUTH_TOKEN there is what makes npm take
      # the token path with no credential and 404 instead of falling back to OIDC.
      - name: Publish
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          if [ -n "$NPM_TOKEN" ]; then
            echo "Publishing with an automation token."
            echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > ~/.npmrc
          else
            echo "No NPM_TOKEN secret - publishing via OIDC trusted publishing."
          fi
          npm publish --access public
